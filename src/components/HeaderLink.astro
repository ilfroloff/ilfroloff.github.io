---
interface Props {
  href: string;
  icon: string;
  label: string;
}

const { href, icon, label } = Astro.props;
---

<a href={href} class="flex text-sm font-semibold leading-6">
  <span>{icon}</span><span class="ml-1.5 max-sm:hidden">{label}</span>
</a>
